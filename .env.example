# Copy this to .env in the repo root and fill in real values.
# docker-compose automatically reads a .env file next to docker-compose.yml.

# Optional - sentiment_engine/utils/config.py already has a fallback demo
# key, but supply your own CoinMarketCap key for real market signals.
CMC_API_KEY=
