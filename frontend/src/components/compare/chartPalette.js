// Distinguishing colors for chart series — not user/app data, just a fixed
// visual palette (same pattern as ALLOCATIONS/TICKER_COLORS elsewhere).
export const CMP_PALETTE = ["#5b6ef5", "#9b6dff", "#00c9b0", "#f5b731"];